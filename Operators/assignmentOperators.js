let x = 10; // Is an example of an assignment operator

x = ++x; // Is equal to x + 1. But what if we wanted to add something like 5? We use the Assignment operator += to simplify it

x = x + 5;
x += 5; // Is the simpler way of writing it

x = x * 3
x *= 3; // Is the simpler way of writing it

