// Ternary or Conditional Operator (if a customer has more than 100 points, they are a gold customer -  otherwise set to silver)

let points = 110;
let type = points > 100 ? "gold" : "silver"; // if the points are greater than 100 to be true, return gold, otherwise return silver

console.log(type);