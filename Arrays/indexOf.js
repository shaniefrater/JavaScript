const numbers = [1, 2, 3, 4]

console.log(indexOf(2));        // This will return the index (position of 2 which will be 1)
console.log(indexOf('a'));      // This will AUTOMATICALLY return -1 where 'a' is not an element in the array, therefore -1 by default

console.log(lastIndexOf());     // This will return the last index number of the sequence e.g. if there's 5 in total, the last index will be 4


