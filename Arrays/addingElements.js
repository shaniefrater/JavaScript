// Adding and removing from Arrays
// You can do this using the Push, Pop, Shift, Unshift and Splice methods

const numbers = [1, 2];

numbers.push(3, 4); // Adding 3 & 4 to the END of an array

numbers.pop();      // Removing the LAST element off of an array

numbers.shift();    // Removing the FIRST eleemnt off of an array

numbers.unshift(2); // Adding the number 2 to the BEGINNING of an array

numbers.splice      // Adding a new element in the MIDDLE of the code
                    // numbers.splice(x, y, z);
                    // x = The INDEX of where you want to start e.g. [0, 1, 2, 3]
                    // y = The ELEMENTS you want to add e.g. 4
                    // z = what you want to add


                    