/* Learning objectives:
   - understand the pop(), push(), shift(), unshift(), concat(), reverse(), sort() methods
*/
const animals = ["anteater", "bison", "ant", "elephant"];
const birds = ["pigeon", "robin", "hawk"];

console.log

const all = animals.concat(birds);  // CONCATenate the 2 arrays
console.log(all);

// all.pop();                          // pop the last element off the END of the array
// console.log(all);

// all.push("cat");                    // push a new element onto the END of the array
// console.log(all);

// all.shift();                        // remove the first element from the BEGINNING of the array
// console.log(all);

// all.unshift("mouse");               // add a new element to the BEGINNING of the array
// console.log(all);

// all.reverse();                      // reverse the array
// console.log(all);

// all.sort();                         // sorts the array alphabetically

// MEMORY HOOK - watch this animation - https://simplestepscode.com/array-push-pop-shift-unshift/