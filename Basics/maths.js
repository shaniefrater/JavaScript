// math.round --> Essentially rounds numbers to the nearest whole number (2.5 to 3)
// math.Ceil --> Returns smallest value GREATER than the number, ALWAYS rounds up (3.1 - 4) 
// math.floor ---> returns the lowest value (2.9999 = 2)

// maths.abs --> Negative returns positive, positive returns positive

// .precision 

// number = 10.456 
// console.log(number.precision(3))


// = 10.4 (3)

// .toPrecision - method rounds x amount of places 
// .toFixed - deals with numbers after decimal point