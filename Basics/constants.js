// Constant should be your first choice when defining a variable

const firstName = "Shanie";
console.log(firstName);

// When you need to reassign value later down the line, then let is best practice to use